<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.expensemanagerapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EXPENSEMANAGERAPP">
        <activity android:name=".TaskListDao"></activity>
        <activity android:name=".TaskListRepository" />
        <activity android:name=".TaskListViewModel" />
        <activity android:name=".TaskDetailFragment" />
        <activity android:name=".TaskDetailViewModel" />
        <activity android:name=".TaskDetailRepository" />
        <activity android:name=".TaskDetailDao" />
        <activity android:name=".TaskDatabase" />
        <activity android:name=".TaskAdaper" />
        <activity android:name=".Task" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>